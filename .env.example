# OpenWeatherMap API Key
# 1) https://openweathermap.org 가입 → My API Keys 에서 발급
# 2) 이 파일을 .env 로 복사한 뒤 값을 채운다 (.env 는 커밋되지 않는다)
# 3) 발급 직후에는 401 이 날 수 있다. 활성화까지 최대 2시간 정도 걸린다
VITE_OPENWEATHER_API_KEY=
